---
interface Props {
  categoryName?: string;
}

const { categoryName } = Astro.props;
const placeholder = categoryName 
  ? `${categoryName}のセリフを検索...`
  : 'セリフを検索...';
---

<div class="mb-6">
  <div class="relative">
    <input
      type="text"
      id="search-input"
      placeholder={placeholder}
      class="w-full px-6 py-4 pr-12 rounded-full border-2 text-lg transition-all duration-300 focus:outline-none"
      style="border-color: #e2e8f0; color: #2d3748;"
    />
    <span class="absolute right-6 top-1/2 transform -translate-y-1/2 text-2xl pointer-events-none">
      🔍
    </span>
  </div>
</div>

<style>
  #search-input:focus {
    border-color: #a78bfa;
    box-shadow: 0 0 0 3px rgba(167, 139, 250, 0.1);
  }
</style>