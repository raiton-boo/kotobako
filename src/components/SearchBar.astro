---
interface Props {
  categoryName?: string;
}

const { categoryName } = Astro.props;
const placeholder = categoryName 
  ? `${categoryName}のセリフを検索...`
  : 'セリフを検索...';
---

<div class="mb-6">
  <div class="relative">
    <input
      type="text"
      id="search-input"
      placeholder={placeholder}
      class="w-full px-6 py-4 pr-12 rounded-full border-2 border-gray-200 bg-white text-gray-800 text-lg transition-all duration-300 focus:outline-none focus:border-purple-400 focus:ring-2 focus:ring-purple-100"
    />
    <span class="absolute right-6 top-1/2 transform -translate-y-1/2 text-2xl pointer-events-none">
      🔍
    </span>
  </div>
</div>