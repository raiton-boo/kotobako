---
interface Props {
  id: number;
  text: string;
  category: string;
  featured?: boolean;
}

const { id, text, category, featured = false } = Astro.props;
---

<div class="bg-white rounded-2xl shadow-md hover:shadow-xl transition-all duration-300 p-8 relative transform hover:-translate-y-1">
  {featured && (
    <span class="absolute top-4 right-4 text-xs px-3 py-1 rounded-full font-bold" style="background-color: #fbbf24; color: #78350f;">
      ⭐ おすすめ
    </span>
  )}
  
  <p class="font-serifu text-3xl md:text-4xl text-center mb-6 leading-relaxed" style="color: #2d3748;">
    {text}
  </p>
  
  <div class="flex justify-between items-center mt-6 pt-4 border-t" style="border-color: #e2e8f0;">
    <span class="text-sm" style="color: #a0aec0;">ID: {id}</span>
    
    <div class="flex gap-2">
      <button
        class="p-2 rounded-full hover:bg-gray-100 transition-all duration-200"
        aria-label="お気に入りに追加"
      >
        ❤️
      </button>
      
      <button
        class="p-2 rounded-full hover:bg-gray-100 transition-all duration-200"
        aria-label="シェア"
      >
        🔗
      </button>
    </div>
  </div>
</div>