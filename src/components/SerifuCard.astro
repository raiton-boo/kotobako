---
interface Props {
  id: string;
  text: string;
  category: string;
  featured?: boolean;
  createdAt?: string;
  showCategoryInId?: boolean;
}

const { id, text, category, featured = false, createdAt, showCategoryInId = false } = Astro.props;

let displayId = id.split('-').pop() || id;
if (showCategoryInId) {
  const categoryNames: Record<string, string> = {
    'batsu-game': '罰ゲーム',
    'kokuhaku': '告白',
    'haishin': '配信',
    'chuunibyou': '厨二病'
  };
  const categoryName = categoryNames[category] || category;
  displayId = `${categoryName} No.${displayId}`;
} else {
  displayId = `No.${displayId}`;
}

const formattedDate = createdAt ? createdAt.replace(/-/g, '/') : '';
---

<div 
  id={`serifu-${id}`}
  class="bg-white rounded-2xl shadow-md hover:shadow-xl transition-all duration-300 p-8 relative transform hover:-translate-y-1 cursor-pointer select-none"
  data-id={id}
  data-category={category}
  data-text={text}
  style="height: 280px;"
>
  {featured && (
    <span class="absolute top-4 right-4 bg-yellow-400 text-yellow-900 text-xs px-3 py-1 rounded-full font-bold pointer-events-none">
      ⭐ おすすめ
    </span>
  )}
  
  <div class="absolute inset-0 flex items-center justify-center p-8 pointer-events-none" style="padding-bottom: 80px;">
    <p class="font-serifu text-3xl md:text-4xl text-center leading-relaxed text-gray-800">
      {text}
    </p>
  </div>
  
  <div class="absolute bottom-0 left-0 right-0 flex justify-between items-center p-8 pt-4 border-t border-gray-200 pointer-events-none">
    <div class="flex flex-col gap-1">
      <span class="text-sm font-bold text-gray-400">
        {displayId}
      </span>
      {createdAt && (
        <span class="text-xs text-gray-400">
          Created at {formattedDate}
        </span>
      )}
    </div>
    
    <div class="flex gap-2 pointer-events-auto">
      <button
        class="favorite-btn p-2 rounded-full hover:bg-gray-100 transition-all duration-200"
        aria-label="お気に入りに追加"
        data-serifu-id={id}
      >
        ❤️
      </button>
      
      <button
        class="copy-btn p-2 rounded-full hover:bg-gray-100 transition-all duration-200"
        aria-label="リンクをコピー"
        data-serifu-id={id}
      >
        🔗
      </button>
    </div>
  </div>

  <div class="progress-overlay absolute inset-0 rounded-2xl pointer-events-none opacity-0 transition-opacity duration-200" style="background: linear-gradient(90deg, rgba(167, 139, 250, 0.3) 0%, transparent 0%);">
  </div>
</div>

<style>
  .progress-overlay.active {
    opacity: 1;
  }
</style>